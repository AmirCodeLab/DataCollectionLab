/** View, download, paste or upload the IR — the floor under the editor
 *  (scope §1): the way in and out for anything the builder cannot express.
 *
 * STUB — replaced in step 6.
 */

export function IrMenu() {
  return <span className="text-xs text-slate-500">IR</span>;
}
