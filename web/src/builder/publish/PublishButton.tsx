/** Publish the draft through the one route into `form_version` (scope §6).
 *
 * STUB — replaced in step 6.
 */

export interface PublishButtonProps {
  projectId: string;
}

export function PublishButton(_props: PublishButtonProps) {
  return (
    <button type="button" className="rounded border px-3 py-1 text-sm" disabled>
      Publish
    </button>
  );
}
