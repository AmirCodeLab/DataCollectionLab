/** Ask the server for a compile answer whenever the document changes.
 *
 * STUB — replaced in step 6. The page calls this once; it owns the debounce
 * and reports through the store's `compile*` actions.
 */

export function useAutoCompile(): void {
  // Filled in step 6.
}
