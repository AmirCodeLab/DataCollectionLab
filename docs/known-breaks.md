# Known breaks

Every guarantee in this project is defended by a test. A test is only a defence
if it has been watched to fail. This file is the register of the breaks we have
deliberately introduced to watch them fail, and — just as importantly — the ones
we have not.

The distinction is the whole point. "There is a test for that" and "we have seen
that test catch it" are different claims, and only the second one is worth
anything. Until this file existed the difference lived in chat history, which is
to say nowhere: a break run in a session six weeks ago left no trace in the
repository, so the next person had no way to tell a proven guarantee from a
hoped-for one.

## How to use it

To re-verify a row: apply the change in **Break**, run the command in **Caught
by**, confirm it fails, revert, confirm it passes again. Then update **Verified**
with the date.

Two rules carry over from `docs/project-conventions.md` and matter here:

- **Never leave a break in.** Revert it in the same sitting. A break committed by
  accident is a silent hole in exactly the property this file says is defended.
- **Never fix a failing vector by editing the expectation** (rule 3). If a break
  makes a vector fail, that is the vector working. Reverting the break is the
  fix.

A row marked **unverified** is a guarantee we have only claimed. It is not a bug,
but it is not evidence either, and it should not be cited as though it were.

## The register

| # | Break | Caught by | Guarantee it protects | Verified |
|---|---|---|---|---|
| 1 | **Null coerced to `false` at the relevance boundary.** In `backend/app/modules/form_engine/expression.py`, make `coerce_boolean(..., null_is=True)` return `False` for null — and the same in `Evaluator.coerceBoolean` in `shared/form-engine/src/commonMain/kotlin/com/dcp/form/Expression.kt`. | `conformance/vectors/relevance-001.json` and `relevance-002.json` must fail — on **both** engines: `cd backend && pytest tests/test_conformance.py -k test_vector` and `./gradlew :shared:form-engine:jvmTest` | Spec 4.4: a field whose relevance expression is unknown stays **visible**. Coercing to false silently hides questions the moment a referenced answer is blank, so an enumerator never sees them and the data is missing with no trace of why. | Yes — date not recorded (predates this file) |
| 2 | **`path` removed from the op AAD.** In `backend/app/modules/crypto/envelope.py`, drop `path` from the AAD tuple; same in the Kotlin `EncryptionEnvelope`. | `conformance/crypto/op-value-001.json` must fail, including its `tamper` cases: `pytest tests/test_crypto_conformance.py` and `./gradlew :shared:core:jvmTest` | Envelope §5: a value is bound to its exact location. Without `path`, a server operator can move an encrypted answer from `income` to `age`, or into another form version where the path means something else, and decryption succeeds without complaint. | Yes — date not recorded (predates this file) |
| 3 | **Op nonce made constant.** In `envelope.py`, return a fixed 12 bytes from the op nonce derivation instead of deriving it from `(deviceId, counter)`. | `backend/tests/test_envelope.py::test_op_nonce_is_deterministic_and_correct_length`, `::test_op_nonce_differs_across_devices_and_counters`, `::test_op_nonce_never_repeats_over_a_long_counter_run`, and `conformance/crypto/op-nonce-001.json` | Envelope §4.5. Reusing an AES-GCM nonce under one key is catastrophic, not degraded: two ciphertexts under the same (key, nonce) leak the XOR of their plaintexts and forfeit authentication. The server's `(content_key_id, nonce)` uniqueness constraint is the last line of defence, not the first. | Yes — date not recorded (predates this file) |
| 4 | **Ordering by `wall_clock` instead of `(counter, device_id)`.** In `backend/app/modules/sync/service.py`, change the fold's `ORDER BY`. | `backend/tests/test_sync.py::test_two_devices_converge_regardless_of_arrival_order` must fail (it is built so the two orderings disagree) | Sync §6: last-writer-wins by logical counter, with `deviceId` as the tiebreak. Device clocks in the field are wrong — flat batteries, no NTP, manual changes — so a wall-clock fold makes the answer depend on whose phone was more confused, and two servers replaying the same log can disagree. | Yes — date not recorded (predates this file) |
| 5 | **`sensitive` flag ignored in `field_level` mode.** In `shared/core/.../SyncCrypto.kt`, treat every field as non-sensitive so nothing is encrypted. | `EncryptedSyncTest`: `field_level encrypts only the sensitive fields`, `a repeat path resolves to its field, so members income is still encrypted`, and `field_level encrypts everything when the form version is unknown` — `./gradlew :shared:core:jvmTest` | Envelope §3.1 with sensitivity propagation. `field_level` is a promise that the fields marked sensitive never reach the server readable. Ignoring the flag sends them in the clear while every screen still shows the project as encrypted — the failure is invisible from the console. | Yes — date not recorded (predates this file) |
| 6 | **Private key persisted to storage (console).** In `web/src/pages/SubmissionPage.tsx`, in `loadKeyFile`, after `parsePrivateKeyFile` write the scalar to `localStorage`. | `web/src/pages/SubmissionPage.test.tsx` → `never reaches localStorage, sessionStorage, IndexedDB or a request` — `cd web && npm test` | Envelope §7. The page states in bold that the key is "never uploaded, never stored and never logged". A key in `localStorage` outlives the tab, survives a reload, and is readable by anything that can run script on the origin — so the console becomes a place where a project's private key can be found, which is the one thing it must never be. | **Yes — 2026-08-30** |
| 7 | **The local database driver stops passing the key.** In `shared/core/.../DatabaseDriverFactory.jvm.kt`, make `keyProperties` return an empty `Properties()` — what a build that lost its cipher looks like. | Ten of the twenty tests in `LocalDatabaseEncryptionTest` must fail, including `the database file on disk contains none of the plaintext answers` and `a database created under one key cannot be opened with another` — `./gradlew :shared:core:jvmTest` | Envelope §14.1: a seized or stolen phone. Every answer the device has collected sits in the local op log, and unencrypted that file gives all of them up to anyone holding the handset — for many of this platform's users the likelier attack by a wide margin, and the one thing the §1–§10 envelope does nothing about. | **Yes — 2026-08-30** |
| 8 | **The cleartext guard always answers "encrypted".** In `shared/core/.../LocalDatabaseGuard.kt`, make `isCleartext` return `false` unconditionally. | `LocalDatabaseEncryptionTest`: `a cleartext database is refused rather than used`, `the driver refuses to hand back a driver whose file turned out to be cleartext`, `an existing cleartext database is migrated…`, `a cleartext database with no key is a normal upgrade…` — `./gradlew :shared:core:jvmTest` | Envelope §14.5. SQLite treats an unrecognised pragma as a no-op and returns no error, so a build linked against plain SQLite instead of SQLCipher runs perfectly, passes every functional test, and writes the whole op log to disk in the clear. The header check is the only thing that can see that from inside the app — and it is also what tells the migration a database still needs converting. | **Yes — 2026-08-30** |
| 9 | **The lost-key guard removed.** In `shared/core/.../CoreFactory.kt`, drop the `databaseState() == ENCRYPTED && !keyStore.exists()` check in `createSubmissionStore`. | `LocalDatabaseEncryptionTest`: `an encrypted database with no key in the keystore is refused, not replaced` — `./gradlew :shared:core:jvmTest` | Envelope §14.3: there is no recovery for the local database key, so an encrypted database plus an empty keystore means the key is **lost**, not that this is a first run. Minting a replacement opens a new empty database beside the real one and every unsynced answer on the device becomes unreachable forever — while the app reports itself perfectly healthy and the enumerator sees an empty list. The keystore cannot make this call itself: macOS reports a keychain it cannot open with the same `errSecItemNotFound` it uses for a key that is genuinely absent. | **Yes — 2026-08-30** |
| 10 | **An enum member added to the wire type without regenerating the contract.** Add `"archived"` to `type SubmissionStatus` in `backend/app/modules/submissions/schemas.py` and commit nothing else. | `python scripts/generate_api_contract.py --check` exits 1 naming both files, and `backend/tests/test_openapi_contract.py::test_committed_contract_matches_the_app` and `::test_committed_console_types_match_the_contract` fail — as does `test_wire_enum_mirrors.py::test_submission_status_mirrors_the_check_constraint`, because the database constraint has no `archived` either | The committed contract is what the console, the Kotlin clients and anyone integrating read. A snapshot that has fallen behind the server is worse than no snapshot: it is wrong and it looks authoritative. The byte comparison is what makes "the app is the source of truth" enforceable rather than a convention. | **Yes — 2026-08-30** |
| 11 | **A route written without a `response_model`.** In `backend/app/api/v1/forms.py`, drop `response_model=EvaluateResponse` from `/evaluate` and return `dict[str, object]` — then regenerate the contract, so the byte comparison is satisfied and only the shape rule is left. | `backend/tests/test_openapi_contract.py::test_every_success_response_names_a_schema`, which names the route: `POST /api/v1/forms/evaluate → 200 returns an inline schema` | FastAPI infers an undeclared body from the return annotation, and `dict[str, Any]` infers to an object with **no fields**. That generates as `Record<string, unknown>`: it type-checks in the console, it runs, and it has quietly stopped describing the API. The route looks documented and is not — which is the failure mode a generated contract is otherwise blind to, because regenerating makes the diff go away. | **Yes — 2026-08-30** |
| 12 | **A closed value set declared as a plain assignment.** Change `type SubmissionStatus = Literal[...]` back to `SubmissionStatus = Literal[...]`. | `backend/tests/test_openapi_contract.py::test_every_closed_value_set_is_a_named_schema`, naming the property: `components.schemas.SubmissionDetail.properties.status has an inline enum` | Pydantic inlines an unnamed alias at every use site, so one closed set becomes six anonymous unions the generated client cannot name — and cannot produce a runtime array from. The console renders its status filter and key-role dropdown from those arrays; without them they go back to being hand-written, which is the drift this whole chain removed. | **Yes — 2026-08-30** |
| 13 | **An error model declared as the payload instead of the envelope.** In `backend/app/api/v1/devices.py`, declare `403: {"model": DeviceRegisterError}` in place of `DeviceRegisterErrorResponse`. | `backend/tests/test_openapi_contract.py::test_every_declared_error_body_names_a_schema`, naming the route and the fields it found | This was a real defect in the API before the contract was committed: FastAPI wraps an `HTTPException`'s `detail` in `{"detail": ...}`, so declaring the payload published a body the server has never once returned. A generated client written against it fails on the first refusal it meets — and a refusal is exactly when a client can least afford to be surprised. | **Yes — 2026-08-30** |
| 14 | **The document-shape gate removed.** In `backend/app/modules/form_engine/runtime.py`, drop the `check_document(ir)` call at the top of `CompiledForm.__init__`. | **38 of the 46** assertions in `backend/tests/test_malformed_conformance.py` fail — and `POST /api/v1/forms/compile` answers **500** for a form missing `formId` again, which is how the hole was found. `cd backend && pytest tests/test_malformed_conformance.py` | Form IR §10.1. A statically typed runtime gets this gate free from its deserialiser and a dynamically typed one gets nothing, so the two engines did not differ in their error message — the Kotlin engine refused nine document shapes and the Python engine crashed partway through compilation. The crash is not the worst of it: `KeyError: 'formId'` reaches a form author as an opaque 500, and the same code path is `POST /forms/versions` and `scripts/seed_dev.py`. | **Yes — 2026-08-30** |
| 15 | **`irVersion` accepted whatever it says.** Return early from `check_ir_version` in `backend/app/modules/form_engine/document.py`, and from `checkIrVersion` in `shared/form-engine/.../Ir.kt` — one break, both engines, because this one was a hole in both. | Vectors `malformed-017` (major 9.9), `-018` (higher minor 0.2) and `-019` (not a version number) fail **on both engines and only those three**: `pytest tests/test_malformed_conformance.py` and `./gradlew :shared:form-engine:jvmTest` | Spec §9. Neither engine checked the value before this: a form declaring `irVersion: "9.9"` compiled and published. A v0.1 runtime handed a v1.0 document does not know which of the fields it recognises still mean what they used to, so reading what it can produces a form that looks correct and evaluates by the wrong rules — with no error anywhere, which is the failure this specification exists to prevent. The enumerator whose device is a version behind has to be told to update; a form that opens and quietly misbehaves tells them nothing. | **Yes — 2026-08-30**, re-run the same day: an early return from both `check_ir_version` and `checkIrVersion` fails `malformed-017`, `-018` and `-019` and only those three, identically on both engines (Python 6 failed of 46, Kotlin 6 failed of 85). Note a *narrower* break — disabling only the `major/minor` comparison and leaving the not-a-number guard — fails `-017` and `-018` but **not** `-019`, so a partial revert of this check would still look two-thirds caught. |
| 16 | **Staging writes plaintext to disk.** In `shared/core/.../MediaStaging.kt`, change `files.write(mediaId, chunkIndex, encrypted)` to write `plaintext.copyOfRange(offset, end)` instead — what a capture path that forgot to encrypt looks like. | Three of the seven tests in `MediaStagingTest` fail, including `a staged file on disk contains none of the original image bytes` (the marker from the original image is found verbatim) and `a standard-mode project still encrypts the file on this device` — `./gradlew :shared:core:jvmTest` | Envelope §6.1. A photograph of an identity document sitting in cleartext on a phone defeats everything §14 does for the database beside it. The order — compress in memory, encrypt in memory, then write — is the whole guarantee, and it is invisible from every screen: the app works perfectly either way, and the difference only shows on a seized handset. | **Yes — 2026-08-30** |
| 17 | **The device trusts its own record of which chunks landed.** In `shared/core/.../MediaUploader.kt`, delete the `store.replaceChunkState(media.mediaId, session.receivedChunks)` line so the local chunk state is kept instead of being replaced by the server's answer. | `MediaUploaderTest`: `an interrupted upload resumes without re-sending completed chunks` fails with `expected:<[2]> but was:<[0, 1, 2]>`, and `the server's chunk list wins over the device's own record` fails with `expected:<[1, 2]> but was:<[]>` — `./gradlew :shared:core:jvmTest` | Sync §9. The server decides whether `complete` will succeed, so its list is the only authority on what still has to be sent. A device trusting its own record skips a chunk the server never received and then fails completion with no way forward but starting over — on the connections resumable upload exists for, that is an upload that never finishes. Note the first attempt at this break (replacing the *use* of `receivedChunks` while leaving `replaceChunkState` in) was **not** caught, because the local record had already been overwritten with the server's: the load-bearing line is the write, not the read. | **Yes — 2026-08-30** |
| 18 | **A route's `response_model` changed without regenerating the contract.** In `backend/app/api/v1/media.py`, change `PUT /upload-sessions/{id}/chunks/{n}` from `response_model=MediaChunkResponse` to `MediaCompleteResponse` and commit nothing else. | `python scripts/generate_api_contract.py --check` exits **1**, naming both `specs/openapi.json` and `web/src/api/types.ts`; `backend/tests/test_openapi_contract.py::test_committed_contract_matches_the_app` and `::test_committed_console_types_match_the_contract` fail (2 failed, 5 passed) | The same guarantee as break 10, reached from the other direction: 10 changes a *schema*, this changes which schema a *route* returns. Both have to be caught, because a route silently returning a different shape is the change a console reads as working until the field it wants is missing. Confirms the byte comparison is not keyed to enum members alone. | **Yes — 2026-08-30** |
| 19 | **The media content hash computed over plaintext.** In `shared/core/.../MediaStaging.kt`, change `ciphertextHash = envelope.ciphertextHash(ciphertextChunks)` to `envelope.ciphertextHash(listOf(plaintext))`. | `MediaStagingTest`: `the content hash is over ciphertext, so one photograph staged twice differs` and `MediaUploaderTest`: `a standard-mode project uploads plaintext the server can read` — 2 failed of 90, `./gradlew :shared:core:jvmTest` | Envelope §6. Hashing plaintext would deduplicate neatly and would also let the server prove two submissions contain the same photograph — exactly the inference end-to-end encryption exists to prevent. It is invisible from every screen: uploads succeed, `complete` verifies, and the only symptom is that two devices photographing the same ID card produce the same hash. This is the client half of the guarantee whose server half is `test_the_same_photograph_from_two_devices_has_different_ciphertext_hashes`. | **Yes — 2026-08-30** |
| 20 | **Navigation gated on validity.** In `shared/form-engine/.../Screens.kt`, make `nextScreen` return null when any question on the `from` screen is invalid — a UI rule pushed down into the engine, which is the charitable version of the drift. | `conformance/vectors/screens-004.json` fails on Kotlin and **only** that vector, alongside three of the five `NavigatorTest` tests: 4 failed of 95, `./gradlew :shared:form-engine:jvmTest`. Python stays green, which is the point — the vector is what makes a one-sided change visible. | Form IR §6.2. An enumerator who cannot move past a question the respondent refuses to answer invents an answer, and an invented answer is worse than a gap because nothing downstream can see it. Before §6.2 this was not a wrong rule but an **absent** one: `Screens.kt` had no concept of valid or required, the spec did not say, and the clients had each decided for themselves. | **Yes — 2026-08-30** |
| 21 | **The same gate, one level higher.** Leave `nextScreen` alone and put the validity check in `FormNavigator.next()` instead — where a client-shaped fix would actually land. | **All 39 vectors pass on both engines.** Only `NavigatorTest` fails: 3 failed of 95, `./gradlew :shared:form-engine:jvmTest` | The reason `NavigatorTest` exists. `FormNavigator` is the cursor every client drives and it is Kotlin-only — the Python reference has no interactive cursor — so no cross-engine vector can reach it. A gate added there ships a client that will not let an enumerator past an unanswered question, with a fully green conformance run behind it. Watching break 20 fail says nothing about this layer; they are separate rows for that reason. | **Yes — 2026-08-30** |
| 22 | **Severity ignored by the finalisation gate.** In `backend/app/modules/form_engine/screens.py`, change `blocking_fields` to `if state.relevant and not state.valid`. | `conformance/vectors/screens-006.json` fails on Python and only that vector — `screens-006 step 0: screens.canFinalize expected True, got False`, 1 failed of 40, `cd backend && pytest tests/test_conformance.py` | Form IR §6.1. A soft constraint is a question the enumerator is meant to be able to override — "400 kg, please confirm" — and a gate that reads `not valid` refuses to finalise the submission instead, with no override to offer because none is built (§6.1 says so). The difference is invisible until a form ships a `severity: warning` constraint, at which point the submission cannot be sent and nothing on screen explains why. | **Yes — 2026-08-30** |
| 23 | **The date field's click overlay removed.** In `clients/composeApp/.../CollectionScreen.kt`, change the `if (enabled)` guarding the transparent `selectable` Box over the date field to `if (false)` — the click then lands on the read-only `OutlinedTextField` underneath, which focuses and does nothing. That is defect 1's reported symptom exactly. | **Four of the five** tests in `DateQuestionTest` fail — `aMouseClickOnTheFieldOpensThePicker`, `aTouchOnTheFieldOpensThePicker`, `confirmingThePickerEmitsTheSelectedDate`, `clearingThePickerErasesTheAnswer`: 4 failed of 5, `./gradlew :clients:composeApp:jvmTest` | A date question can be answered at all. The widget is a transparent click target laid over a field that is deliberately `readOnly`, so the answer path depends entirely on hit-test order between two overlapping composables — and nothing else in the repository can see that. The conformance vectors cannot: they test the engine, and a form whose date question cannot be opened evaluates perfectly. The fifth test, `aDisabledFieldDoesNotOpenThePicker`, correctly keeps passing under this break, since it asserts the picker's **absence** — worth noting, because a suite where every test fails on one break is usually a suite testing one thing. | **Yes — 2026-08-30** |
| 24 | **A suite removed from CI, nine ways.** Against `scripts/check_ci_runs_every_suite.py`: (a) delete the `shared UI tests` step so `:clients:composeApp:jvmTest` is unrun — the real failure; (b) replace the `pytest -m db` step with an `echo`, so the marker split stops covering 42 tests; (c) narrow `test.include` in `web/vite.config.ts` to `src/pages/**` so `harness.test.ts` is not collected; (d) create `shared/core/src/wasmJsTest` — a source set the script has never heard of; (e) add `:clients:desktopApp:test`, which has no sources, to the kotlin job; (f) rename web's `lint` script; (g) delete `web/eslint.config.js`; (h) add `shared/form-engine/src/iosTest`, which no Linux runner can execute; (i) delete `ci.yml` entirely. | Each fails with the suite named: `python scripts/check_ci_runs_every_suite.py --strict` exits 1 on (a)–(h) and refuses outright on (i). Nine breaks, nine distinct messages, one at a time. | That CI runs every test in the repository. This has been false three times — the workflow was uncommitted, `npm run lint` exited 0 with no eslint config, and `:clients:composeApp:jvmTest` was absent — and not once did it show up as a red build. That is the whole danger: an unrun suite does not look like a gap, it looks like coverage, so the repository reads as better defended than it is. Note (d) and (h) are the guard's own decay modes rather than CI's: an unrecognised source set and a suite no runner can execute both **fail**, because a guard that quietly passes what it does not understand becomes the thing it was written to prevent. And (i) is the one case it cannot catch from inside CI — no workflow, no job — which is why `./scripts/status.sh` section 5 asks the same question locally. | **Yes — 2026-08-30**, all nine |
| 25 | **Retention narrowed to "the server's word".** In `shared/core/.../forms.sq`, drop the `NOT EXISTS (SELECT 1 FROM submission ...)` clause from `deletableFormVersions`, so a version is prunable as soon as the server stops deploying it. | Two tests, one per layer: `FormStoreTest`: `pruning keeps a withdrawn version a draft was collected under` (`nothing is prunable while a submission refers to v1 expected:<0> but was:<1>`) and `FormDeliveryTest`: `a withdrawn version stays on the device while a draft still needs it` — 2 failed of 108, `./gradlew :shared:core:jvmTest` | Form IR §9: a submission is validated against the version it was collected under. An enumerator can be half way through a v2 interview on the morning v3 deploys, and a device that dropped v2 has a draft it can no longer open — the op log records the answers, never the questions. The failure is invisible until someone reopens that draft, by which time the sync that destroyed the form looks like the successful one it reported itself to be. | **Yes — 2026-09-02** |
| 26 | **The form manifest ignores the environment.** In `backend/app/modules/forms/service.py`, drop `FormDeployment.environment_id == environment_id` from the `deployed_versions_for_device` query, keeping only the `retired_at IS NULL` filter. | `backend/tests/test_form_delivery.py::test_a_staging_deployment_does_not_reach_a_production_device` and only that one — 1 failed of 13, `pytest tests/test_form_delivery.py`. The failure prints the manifest, so the leaked form (`staging_only`) is named rather than inferred. | Deployment is per environment (`form_deployment`), and it is the only thing standing between a form still being tested and every phone in the field. A manifest that ignores it makes staging and production the same place while both still appear in the console, so the mistake is invisible from the server that made it. | **Yes — 2026-09-02** |
| 27 | **A revoked or unknown device falls back to some other device's forms.** In `deployed_versions_for_device`, replace the `return None` for an unknown/revoked device with a lookup of the first device in the table. This is the shape of the tempting fallback — "answer with something rather than nothing". | `backend/tests/test_form_delivery.py::test_a_revoked_device_is_told_about_no_forms` — 1 failed of 13 | A device the server will not accept data from has no business learning a project's forms; the same rule `/devices/{id}/media-policy` already holds. Any fallback here also silently makes `deviceId` optional in practice, which takes the environment rule (break 26) with it — a manifest that will answer *somehow* is a manifest whose scoping nobody can rely on. | **Yes — 2026-09-02** |
| 28 | **An absent `forms` key read as an empty manifest.** In `shared/core/.../SyncWire.kt`, change `WirePullResponse.forms` from `List<WireDeployedFormVersion>? = null` to `? = emptyList()` — collapsing the two answers into one, which is what a defaulted field does and why this one is nullable. | `FormDeliveryTest`: `a server that does not serve forms leaves the device's forms alone` — 1 failed of 108, `./gradlew :shared:core:jvmTest` | A self-hosted install runs whatever version it runs, and a server older than form delivery sends no `forms` field at all. Reading that silence as "your environment deploys nothing" marks every form on the device undeployed, and the enumerator opens the app to a list that offers no interview to start — caused entirely by a successful sync against a server that did nothing wrong. `"forms": []` is a real answer — this environment deploys nothing — and it is acted on: the pair `a server that does not serve forms leaves the device's forms alone` and `a project that withdrew its last form does undeploy it` is what pins the two apart, and a single defaulted field cannot satisfy both. | **Yes — 2026-09-02** |
| 29 | **Held versions compared on (formId, version) instead of the checksum.** In `shared/core/.../FormStore.kt`, make `missingFrom` compare `it.formId to it.version` against what is held rather than `irChecksum`. | `FormStoreTest`: `content that drifted under the same version number is refetched` (`expected:<[fv-household-1]> but was:<[]>`) — 1 failed of 108 | The server refuses to republish a version number with different content, so this is a defence in depth — against a restored backup, a reseeded database, or a hand-edited row. Comparing on the number alone means a device that holds *a* v1 never notices it is holding the wrong v1, and collects against a form the server has no record of while every screen reports it as current. | **Yes — 2026-09-02** |
| 30 | **The client opens a submission against the newest form version it holds** instead of the one the submission was collected under. Originally in `CollectionViewModel` (where the choice used to live); now only expressible in `FormCatalog.compiledFormForSubmission`, since the ViewModel is no longer given a version. | `FormVersionBindingTest`: `a draft opens against its own version, not the newest one delivered` — `expected:<1> but was:<2>`, 1 failed of 10, `./gradlew :clients:composeApp:jvmTest` | Form IR §9. **This break was run first with no test in the repository at all, and all 462 tests passed** — 39 vectors on both engines, `:shared:core`, `:clients:composeApp` and the backend — while the same build on an emulator rendered a v1 draft under v2's labels over v1's answers. That is the whole shape of the gap docs/project-conventions.md describes: the vectors compare two engines and there is no second client to compare against, `:shared:core` knows about versions but not about which one a screen opens, and the symptom is silent — the answers are still there, the questions merely changed, and nothing on screen says the form is not the one the interview started on. The fix removed the choice rather than only testing it. | **Yes — 2026-09-02**, both ways: caught by nothing before `FormVersionBindingTest` existed, caught by it after |
| 31 | **A Form IR document packaged into the APK**, three ways: (a) drop `specs/examples/household_survey.json` into `clients/composeApp/src/commonMain/composeResources/files/`; (b) do that, delete it again, and rebuild **without** cleaning -- the stale-build route; (c) embed a document as a Kotlin string constant, so it lands in the DEX string pool rather than in `assets/`. | `:clients:androidApp:verifyNoBundledFormDebug`, which `assembleDebug` and `installDebug` both depend on. Each break names the entry it found rather than reporting a count: (a) and (b) `androidApp-debug.apk -> assets/composeResources/.../files/household_survey.json`, (c) `androidApp-debug.apk -> classes3.dex`. `./gradlew :clients:androidApp:assembleDebug` | Form delivery's central claim is a claim about a binary: there is no form in the APK, so a form on a phone can only have come from the server (sync §5). It had been checked once, by hand, on one build. **Break (b) is not hypothetical -- it happened while this guard was being written.** Deleting the form from the source tree left three stale copies under `build/` (`copyAndroidMainComposeResourcesToAndroidAssets`, `mergeAndroidMainAssets`, `mergeDebugAssets`); the next `assembleDebug` packaged one, and only a `clean` cleared it. Nothing about that is visible from outside: the app syncs, receives forms, and has also shipped with one, and the only symptom is that a device which never synced can still collect -- the exact thing the hand verification was supposed to have ruled out. The guard looks for the *document* (`"irVersion"` followed by a colon, mandatory per §10.1) rather than for the seed form's name, because the name check does not hold on this tree either: `SubmissionListScreenPreview` puts `household_survey` in a `@Preview` literal and previews compile into the APK. A string is not a form, and a guard that cannot tell them apart is one somebody switches off. | **Yes -- 2026-09-02**, all three |
| 32 | **The configurable server address, five ways.** (a) `SyncClient` holds the address at construction instead of asking for it each pass (`val heldForever = serverUrl()`) -- what it did before this change; (b) the opposite, reading `serverUrl()` per *request* rather than once per sync; (c) `ServerConfig.baseUrl()` writes the platform default into the row when nothing is stored, collapsing "nobody has configured this" into "configured"; (d) `FormCatalog.held()` answers with `forms.startable()` rather than `forms.all()`; (e) the settings screen drops the "No address has been set on this device" line. | One test each, and only that one: (a) `SyncClientTest`: `a changed address is picked up by the next sync, with no new client`; (b) `SyncClientTest`: `an address changed mid-sync does not split one sync across two servers`; (c) `ServerConfigTest`: `a fresh device answers with the platform default and says it is the default` plus `reset goes back to the platform default...` -- 2 failed; (d) `HeldFormsTest`: `a withdrawn version a draft still needs is listed, and marked withdrawn` and `startable is the narrower list...`; (e) `SettingsScreenTest`: `aDeviceNobodyHasConfiguredSaysSo`. `./gradlew :shared:core:jvmTest :clients:composeApp:jvmTest` | Every one of these is silent, and (a) and (b) are silent in opposite directions -- which is why both are pinned. **(a)** makes the settings screen appear to work and change nothing until the app is restarted, on the one screen whose purpose is to fix an address that is not working. **(b)** lets one sync span two servers: `refreshCrypto` caches the recipient set of the project the *server* names and the push encrypts to it, so the device would wrap content keys to one project and hand the ciphertext to another, which stores it, reports success, and is holding answers only a third party's private key can ever open (envelope §4.3). **(c)** removes the only way to tell a phone nobody has configured from one somebody set deliberately -- the two need different next steps and look identical. **(d)** makes the screen built to explain a confusing device lie about what the device has: a version withdrawn on the server but retained for an open draft (Form IR §9) would be reported absent, and it is the easy substitution because `startable()` already existed and looks right on every device until the day it matters. **(e)** is the same class as (c), one layer up. None is reachable by a conformance vector: there is no second implementation of a device. | **Yes -- 2026-09-02**, all five |
| 33 | **The settings screen's form list read once instead of observed.** In `FormCatalog.observeHeld`, replace `forms.observeAll()` with `flowOf(forms.all())` -- one emission, at construction, which is what the code did when it was first written. | `HeldFormsTest`: `a form delivered after the list is first read shows up without a relaunch` -- 1 failed of 4, `./gradlew :clients:composeApp:jvmTest`. The test waits under `withTimeout` rather than spinning, deliberately: the break makes the flow emit once and never again, and an unbounded `while (…) yield()` hangs the build instead of failing it -- which was the first version of this test, and it hung. | **This one was not constructed -- it was found on a device**, during the Phase 2 item 1 verification, and it is the reason the screen is now driven by a flow. A fresh install was pointed at a server, synced successfully, and the settings screen went on reporting *"None. This device has synced, so its project has no form deployed to this device's environment"* while the form sat in the database underneath it. There was no error anywhere: the sync was green, the server's manifest was correct, the document had been fetched. Force-stopping and relaunching the app "fixed" it, which is the worst available symptom -- it reads as an intermittent sync fault and sends whoever is holding the phone to check the network and the server, neither of which is wrong. A screen built to explain a confusing device must not be able to contradict the database it is reporting on. | **Yes -- 2026-09-02**, and observed in the field before it was ever written down |
| 34 | **A form document that will not download, skipped in silence.** In `SyncClient.refreshForms`, drop the `undelivered += ...` line from the catch, so the fetch failure is a bare `continue` again -- which is what it was. | `FormDeliveryTest`: `a document that will not fetch does not cost the device the others` -- 1 failed of 111, `./gradlew :shared:core:jvmTest`. The test already covered the good half (the other forms still arrive, the sync still succeeds); what it did not cover, and now does, is that the one which did not arrive is named. | The skip itself is right -- one unreachable form must not cost the device the others -- but an unreported skip erases the entry completely: `applyManifest` has no document to upsert and `markDeployed` updates no row, so the version is simply not there. The sync then reports success, the device holds no form, and nothing anywhere distinguishes that from a project which has deployed nothing. Found by writing the settings screen, which says that second diagnosis out loud -- a screen that states a conclusion is a much harder test of the data behind it than a screen that shows a list, and this is the second silent hole the same screen turned up (the first is break 33). | **Yes -- 2026-09-02** |
| 35 | **The stored address ignored, and the compile-time constant wired back in.** In `AppGraph`, pass `{ defaultSyncBaseUrl() }` to `SyncClient` and `MediaUploader` instead of the `ServerConfig`. | **Nothing, when it was first run — 264 tests, 0 failures**, plus a green APK guard and a clean `status.sh`. That is the finding, and it is recorded here rather than quietly fixed. On the handset the same build showed *"Saved. The next sync will use this address"* under `http://192.168.2.44:8001` and then synced to `http://10.0.2.2:8000`; **zero requests reached the LAN server**. It is now a **compile error**: `Argument type mismatch: actual type is '() -> String', but 'ServerConfig' was expected` at both call sites. | Every test in the repository tested a `SyncClient` it had handed an address to, so none of them could see the app hand it the wrong one — the mistake lived in the wiring, which nothing constructed. The fix is the one break 30 used for form versions: **stop the caller choosing.** `SyncClient` and `MediaUploader` take the `ServerConfig` rather than a `String` or a `() -> String`, so the only address they can use is the configured one, and handing them anything else means constructing a second `ServerConfig` beside the real one. The test source set keeps a `fixedServerConfig(url)` helper for tests that do not care; production cannot reach it. A test would have caught this one instance; the type makes the whole class of it unexpressible, which is the difference between knowing about a defect and having removed it. | **Yes -- 2026-09-02**, both ways: passed by all 264 tests before, refused by the compiler after |
| 36 | **The collectable-types registry and the app disagree**, three ways: (a) delete the `else -> UnsupportedAnswer(...)` branch from `CollectionScreen`'s `when (question.dataType)` -- the original defect 7; (b) delete the `"select_multiple"` branch while leaving it listed in `specs/collectable-types-v0.1.json` -- a type the importer promises and the app cannot ask; (c) the reverse, remove `select_multiple` from the registry while keeping the branch -- a type quietly working that the report calls unavailable. | `CollectableTypesTest` (`:clients:composeApp:jvmTest`), which drives the real composable rather than comparing two lists: (a) 3 failed of 5 -- `every spec type the registry omits says so on screen`, `an unknown type from a newer app version also says so`, `the unsupported message is not a control that takes input`; (b) 1 failed -- `every type the registry lists can actually be answered`; (c) 1 failed -- `every spec type the registry omits says so on screen`. | Defect 7 was this with no defence at all: the `when` covered eight types, had no `else`, and anything else drew its label and empty space -- no control, no message, no disabled state. `select_multiple` is in the IR spec §2.1, has conformance vector `select-001`, and is evaluated identically by both engines, so it compiled, passed all 39 vectors, published, deployed and synced, and arrived as a question nobody could answer. The engines were not what was wrong, which is why nothing below the UI could see it. **The `else` matters more than the two widgets it was found with**: it is what a phone does when a server deploys a form built for a newer app version, which stops being hypothetical the moment customers author their own forms. **Watching (c) fail also turned up a second hole.** The first attempt at it passed, because the test reads `specs/` at run time and Gradle could not see those files as inputs: the edit left the task `FROM-CACHE` and the build green having run nothing. `--rerun-tasks` failed it. The spec files are now declared inputs of the test task, so all three directions fail on an ordinary run. A mirror test that can go stale-green is the suites guard's problem in miniature. | **Yes -- 2026-09-02**, all three, and the stale-green under (c) separately |
| 37 | **Either half of the collectable-types mirror deleted.** (a) `rm clients/composeApp/.../CollectableTypesTest.kt`; (b) `rm backend/tests/test_collectable_types.py`. | Each half asserts the other exists and still reads the registry: (a) `test_the_kotlin_half_of_this_mirror_still_exists` -- 1 failed of 9, naming the missing path; (b) `the python half of this mirror still exists` -- 1 failed of 6. | The suites guard answers "is this suite run by CI", and both halves live in suites it already tracks, so a deleted CI *step* is caught. A deleted test *file* is not -- the suite reports one fewer file and stays green, which is exactly the quiet way a mirror test stops running, and this repository has watched that happen three times. Pointing the two halves at each other closes it without teaching the guard about a specific file. **Break (b) did not fail on the first attempt**, and the reason is the same stale-green as break 36: the Kotlin test reads a file outside its module, Gradle could not see it as an input, and the task was UP-TO-DATE. That recurred *immediately* after fixing it for the registry, in the very commit that fixed it -- the read is easy to add and the input declaration is easy to forget. The lesson is written where it can be acted on, in the build file beside the declaration. | **Yes -- 2026-09-02**, both halves, plus the stale-green under (b) |
| 38 | **A question with an unknown type dropped in silence.** In `xlsform/importer.py`'s `_data_type`, replace the `unsupported_type` diagnostic with a bare `return None, None`, so the row produces no node and says nothing. Two variants: (A) with `ledger.consume(type_cell.ref)`, the tidy version a developer would actually write; (B) without it. | `CoverageHole` — 9 failed of 252, `pytest -m 'not db'`, with the importer refusing outright: *"8 survey row(s) produced no node and no diagnostic, the first being row 16."* Plus the two behavioural tests that named the loss directly (`test_an_unknown_type_names_the_question_it_lost`, `test_the_ucl_form_names_what_it_needed_that_we_lack`). | **Run first, the ledger did not catch either variant**, and that is the finding. Cell-level accounting asks "was every cell consumed or reported", and the code that drops a row tidies its own cells away on the exit path — `_consume_remaining(quiet=True)` marked them accounted for and the residue was empty. The ledger was satisfied and a question was gone: the exact failure it exists to prevent, one level up from where it was looking. Only two ordinary tests caught it, and those can only fail on a construct somebody thought of. Two changes close it. **Rows are accounted for as well as cells**: a survey row must produce a node or carry a diagnostic. And **suppression is separated from explanation** — `ledger.suppress()` marks a cell accounted for without claiming its row was explained, because tidying a dropped row's cells away had been indistinguishable from saying why it was dropped. | **Yes -- 2026-09-02**, both variants, and the ledger's own gap found by running it |
| 39 | **Choice membership, four ways, on both engines.** (a) `select_one` accepts any value — return `[]` unconditionally from the select_one branch; (b) validate against the newest form version rather than the bound one; (c) trim and lowercase both sides before comparing; (d) treat `null` as a value that is not in the list. | `conformance/vectors/choice-00*`, on both engines, identically: (a) 002, 006, 009 — 3 failed of 49 Python and 48 Kotlin; (c) 006 and 007 and nothing else; (d) 003 and nothing else. **(b) is not expressible at engine level and is reported as a hole below.** | Before these vectors, neither engine read `choices` at all. A `select_one` could hold `"purple"`, a `select_multiple` could hold `"unicorn"`, and both engines called the form valid and finalisable — 39 vectors, 270 Kotlin tests and two rounds of review never saw it, because not one vector ever set a value outside its list. The gate was untested because nothing had ever knocked on it. (c) is why §6.3 states exact matching as a decision rather than leaving it to `==`: a device that accepted `"Male"` for `"male"` would **store** `"Male"`, and every later comparison would have to make the same allowance or disagree with it. (d) is §4.4's boundary — an unanswered question is `required`'s business, never `choice`'s. | **Yes -- 2026-09-03**, (a), (c), (d) on both engines; (b) see row 40 |
| 40 | **The version vectors cannot catch binding to the wrong version, and that is the hole.** Break (b) of row 39 — "validate against the latest form version" — has no engine-level expression. An engine is handed one compiled form and never chooses; `choice-008`/`choice-009` make v1 and v2 *disagree* about the value `carol`, which is necessary for any caller-level test to be possible, but no change to either engine makes only `choice-009` fail. It fails alongside 002 and 006 under break (a), because it is an ordinary membership vector as far as an engine is concerned. | Nothing, by construction. Demonstrated rather than tested: running `carol` against v1's form gives `valid=true` and against v2's gives `valid=false`, which is exactly what the pair asserts and exactly as far as a vector can reach. | Recorded because the set would otherwise look complete. This is the same structural boundary as break 30 — version binding lives above the engine — and it means the vectors buy *detectability* rather than detection: without the pair, a caller binding to the wrong version would produce the same answer either way and nothing anywhere could notice. `FormVersionBindingTest` covers the client. **The server has no such test and no such binding**: the push path compiles no form at all, so when §6.4 enforcement lands it needs its own test that a submission is validated against its own version — otherwise the server checks v1 answers against v2's list and rejects data that was correct when it was collected. | **Yes -- 2026-09-03**, at the callers, which is the only place it can be watched. Client: break 30. Server: `compiled_form_for_submission` made to return the *newest* version of the submission's form instead of its own — `test_server_version_binding.py::test_a_v1_answer_is_accepted_where_v2_would_reject_it` fails with *"a submission collected under v1 must still accept `carol`. Rejecting it would destroy an answer that was correct when it was given."* The vector gap stands; the callers are covered. Stated so the vector set is not mistaken for more than it is |
| 41 | **A conformance vector that exists and is never executed.** Add a vector file to `conformance/vectors/` without re-running the Kotlin suite — or simply add one while `:shared:form-engine:jvmTest` is UP-TO-DATE. | `scripts/check_ci_runs_every_suite.py`, which now compares the vector files on disk against the ids the runners actually reported: *"1 evaluation vector(s) exist and the Kotlin run did not execute them: choice-999"* — exit 1. With no results file at all and `--strict`, it fails with *"cannot tell whether the evaluation vectors ran"* rather than passing. | **This happened.** Adding the nine choice vectors left `:shared:form-engine:jvmTest` UP-TO-DATE, reporting 39 tests and BUILD SUCCESSFUL, because both shared modules read `conformance/` at run time and declared no Gradle inputs. It is the third instance of that hazard (36, 37) and by far the worst: **rule 2 is the strongest guarantee this repository makes and it rests entirely on the vectors being executed.** A vector that is not run is indistinguishable from one that passes, and the only thing that caught it was someone reading a test count. Two fixes: `conformance/` and `specs/` are declared as input *directories* in all three modules — directories, so a new vector needs no build change to be noticed — and the suites guard now checks execution rather than trusting it. | **Yes -- 2026-09-03**, both the stale-suite case and the never-ran case |
| 42 | **A form version published against nothing.** Remove the `_resolve_dataset_pins` call from `forms.service.publish_version` and pass whatever the caller sent straight through — the shape a tidy-up takes, since the resolver looks like validation of an already-validated request. A form naming three `choices.dataset` keys then publishes with zero rows in `form_version_dataset`. | `backend/tests/test_form_dataset_pinning.py`, `-m db`: 7 of its 12 fail, naming the unpinned key, the cross-project pin, the swapped pin and the double pin. **The whole non-db suite stays green — 310 passed — and so does every conformance vector.** That is the point of the row: this is above the line the vectors reach, and the IR is valid, compiles, and both engines agree about it either way. | Form IR §3. The IR names a dataset by *key* and a key is not a version, so an unpinned key has to be resolved when the form is opened — against whatever is newest. Same failure as breaks 30 and 40 one level out: an answer validated against a list nobody can reconstruct, with nothing anywhere saying so. | **Yes — 2026-09-03** |
| 43 | **A companion file that did not arrive, reported once.** In `importer._data_type`, drop the `question_without_its_dataset` knock-on so only the first question naming a missing CSV is reported — which reads like sensible de-duplication, and is how it was first written. | The coverage ledger, on the real UCL form: `import_workbook` raises `CoverageHole` — *"3 survey row(s) produced no node and no diagnostic, the first being row 57"* — and `test_no_real_form_produces_a_coverage_hole[ucl-biomass.xlsx]` fails. Found this way rather than by review. | The ledger's own rule, one level out (`workbook.py`). Three questions were gone from the form with nothing pointing at their rows: a `select_one_from_file` whose file did not arrive has *no* options, and a form with an unanswerable question is a perfectly valid form that nothing downstream can question. | **Yes — 2026-09-03** |
| 44 | **`int()` on a text value, which no vector had ever asked about.** Not introduced — *found*, by `dataset-005` and `dataset-006`, on shipped code. The two engines disagreed in opposite directions: Kotlin's `int("800")` returned `null` (`numberOf` accepted only numeric `FormValue`s), and the Python reference's `int("8a")` raised `ValueError`. | `conformance/vectors/cast-00*` on both engines, and `dataset-005`/`006` are what surfaced it: the Kotlin run failed `choices[village] expected [V1] but was []`. | §4.3.1, written *because* of this. A CSV holds nothing but text, so `int($row.population) > 1000` is the ordinary case for a dataset filter — on Kotlin it silently emptied the list, and on Python an unparseable keystroke would have reached the API as a 500. No vector had fed text to a cast because until dataset columns existed nothing in the corpus could. | **Yes — 2026-09-03**, found rather than injected |
| 45 | **An engine that resolves the right list the slow way.** In `candidate_rows` / `candidateRows`, ask the source for every row (`selector = {}`) and apply the selector engine-side afterwards. The decomposition is still right, the resolved list is still right, the count of narrowed rows is still right — and every keystroke scans the whole dataset. | `conformance/vectors/dataset-003/005/009/010` on both engines: 4 of 70 fail on `selector[...] — this is what the source was asked for, not what the engine computed`. **The first attempt at this row did not catch it**, because `candidates` was read off `candidate_rows()`, which is the engine's output; the harness now records what the *source* was asked, on both sides (`RecordingDatasetSource`). A performance contract asserted against the thing being measured measures nothing. | Form IR §3.2. Nothing else in the repository can see this: the answer is correct on every input, so no other test, no type and no review of the diff distinguishes it from the fast version. It shows up only as a village select that takes 400 ms per keystroke on a 2 GB handset. | **Yes — 2026-09-03** |
| 46 | **The whole §4.3 function surface against text, which nothing had ever done.** Not introduced — *swept for*, after break 44 showed one cast disagreeing. Every function and operator was run against every value shape on both engines and diffed: **762 of 1,395 probes disagreed**. Most were one engine raising while the other returned null; the worst were both returning a value and the values differing (`concat(true,true)` was `"TrueTrue"` and `""`; `min(["a","b"])` was `"b"` and null; `round(1234,-2)` was 1200 and 1234). **`regex`, `substr` and `distance` were declared in §4.3, implemented in Python, and had no branch at all in Kotlin** — they fell to `throw CompileException("function not implemented")`, so the UCL biomass form's phone-number constraint would have thrown on a device and passed on the server. | `conformance/functions`, 44 files and 1,419 probes on both engines, plus `fn.spec.json` typed by hand from §4.7's table. Re-running the sweep now reports **0 divergences of 1,395**, representation included, and neither engine raises on any of them. | Form IR §4.7, written because of this. The rule is that an argument of the wrong type is null and evaluation is total apart from integer overflow — an expression is evaluated on every keystroke, on a handset, mid-interview, and an exception there is the app failing rather than the form being wrong. | **Yes — 2026-09-03**, found rather than injected |
| 47 | **A dataset resolved by key instead of by the pin.** In `datasets.sq`, make `rowsForFormVersion` select on `dataset_version.dataset_key` and ignore `form_version_dataset` — "the villages this device has", which is what anybody writes first and which is right in every case except the one that matters. | `DatasetStoreTest`, `:shared:core:jvmTest`: 4 of 14 fail, the first saying *"the form is now pinned to v2 and v2 has not arrived; serving v1 would be a village list that is wrong and looks right"*, and the two-form-version case returning both lists merged. **All 69 conformance vectors and all 382 backend tests stayed green with the break in.** | The stale list has no symptom. The form opens, the list scrolls, the search works, the answer saves and syncs and is accepted, and the enumerator has chosen a village that no longer exists. Which version of a list is used is a *which-artifact* decision, so no vector can see it — docs/project-conventions.md's rule, one level below where it was written. | **Yes — 2026-09-03** |
| 48 | **Dataset rows delivered in ULID order.** Not injected — found by `test_rows_page_resumes_from_its_cursor_and_says_when_it_is_done` on the first run: paging by `dataset_record.id` returned `V028, V01…` for rows published `V000, V001, …`. ULIDs generated in a loop share a timestamp and randomise the tail, so the walk was stable and **scrambled**. | The same test, which asserts the file's own order rather than merely a stable one. Fixed by migration 0005: `dataset_record.ordinal`, which is also the paging cursor. | A choice list is offered in the order its author wrote it. 38,000 villages in an order nobody chose is not a broken list — it is a list every enumerator has to search rather than scroll, on every question, forever, and nothing about it looks wrong enough to report. | **Yes — 2026-09-03**, found rather than injected |
| 49 | **A §4.3 function declared and not implemented.** Delete the `"regex" -> regex(args)` branch from `Functions.call` and leave `"regex" to Sig(2, 2)` in the signature map — which is exactly the state `regex`, `substr` and `distance` shipped in, and `pulldata` was in the table and in neither engine. | `FunctionSurfaceTest` (`:shared:form-engine:jvmTest`), both of its checks: *"`regex` is declared and not implemented"*, and `FunctionConformanceTest`, which calls it. Before this guard existed, **nothing failed**: the four had no vector, `signatures` declared them, and the Python reference implemented three of them so the reference-side tests were green. | The declaration is the trap. `Functions.call` consults `signatures` before dispatching, so a name in the map with no branch in the `when` is a promise the engine does not keep — and a check written against the map would have passed. The symptom is a form that works on the server and throws mid-interview on a phone; the UCL biomass form's phone-number constraint uses `regex`. The guard is by **execution** and its coverage is derived from the spec table, so a new function cannot go unprobed by being forgotten. | **Yes — 2026-09-03** |
| 50 | **A cross-engine float divergence in `distance`.** Not injected — found by `fn.distance.geo.other` the first time it ran: 325481.7667839453 m on Python against 325481.7667839454 m on Kotlin, over identical inputs and an identical formula. | The same probe. Fixed in §4.3 by defining `distance` to millimetres on both engines. | Four transcendental calls deep, `sin`/`cos`/`asin`/`sqrt` are each permitted one ulp of error by both platforms' libraries, so bit-identical results are not something either engine can promise. A millimetre is four orders of magnitude below any GPS fix this platform accepts (§6.1), so the rounding loses nothing real and removes a class of failure no form author could have acted on. | **Yes — 2026-09-03**, found rather than injected |
| 51 | **New tables with no SQLDelight migration.** Not injected — the Pixel found it. `datasets.sq` added three tables, every unit test creates a fresh database from the schema, and all of them passed; the phone, which had the app installed already, threw `no such table: form_version_dataset` on the first line of work. | `MigrationChainTest` catches a *gap* in the .sqm chain and cannot catch a missing one — the schema version is derived from the file count, so adding no migration is self-consistent. **Only an already-installed device shows this**, which is what `scripts/measure_datasets_on_device.sh` was doing when it happened. Fixed by `7.sqm`. | A device in the field carries its op log and its logical counter across upgrades, and operation nonces derive from `(deviceId, counter)` (envelope §4.5). Recreating the database instead of migrating it is not an option, so a missing migration is not a crash on first launch — it is an app that cannot be upgraded into. | **Yes — 2026-09-03**, found on hardware |
| 52 | **An index that fixed the keystroke and broke the sync.** Not injected — measured. Indexing every column of every row (8 × 38,000 = 304,000 entries) took the first keystroke from 1,589 ms to 48 ms, and took the second-sync delta from **137 ms to 14,387 ms**, because applying a delta copies the index across to the new version. | `scripts/measure_datasets_on_device.sh` on a Pixel 6 Pro. Nothing else could have: every unit test runs at a scale where both numbers are single-digit milliseconds. Fixed by indexing only the columns a filter narrows on, which the server names from the IR — 3.2 s first sync, 2.7 s delta, 11.3 MB. | The number that decides field usability is the weekly delta, not the one-off first sync. An optimisation measured on one axis and shipped is how a feature becomes unusable in the direction nobody was looking. | **Yes — 2026-09-03**, found by measurement |
| 53 | **A lookup on a column the index does not cover.** Introduced by 52's fix and caught immediately: with `filter_columns` selective, `rowsMatching` still took the indexed path for any column and returned **no rows** — a filter on an unindexed column resolving to an empty village list on a device holding every village. | `DatasetStoreTest`, `the engine's source serves what the form version pinned and nothing else`: expected 1, got 0. Fixed by checking coverage before taking the indexed path, and falling back to the scan. | An index that does not cover a column answers "no rows" rather than "I cannot help", and the two are indistinguishable downstream. It is the stale-list failure by a different road: nothing is in an error state and the list is wrong. | **Yes — 2026-09-03**, caught by an existing test |
| 54 | **A dataset lookup that scans the index instead of seeking it.** In `datasets.sq`, let the pinned `dataset_version_id` arrive through a JOIN rather than as a literal — which is how it was first written, and reads naturally. | Only a device with **two** versions of a list held: 7.3 ms per keystroke with one version in `dataset_cell`, **77–91 ms** with two, on the same 229-village answer. Linear in the table, which is what §3.2 says it must not be. Every unit test passed both ways; the bench passed both ways; it took a Pixel that had applied a delta. | With the version arriving from a join, SQLite cannot use the leading column of the primary key and walks the whole table. An index that is scanned is not an index — it is a smaller scan, and it degrades exactly as a device accumulates versions, which is to say in the field and not in a demo. | **Yes — 2026-09-03**, found on hardware |
| 55 | **The bidi isolates, removed as cosmetic.** Return `text` from `isolate()` on both engines — two invisible codepoints that look like noise, which is exactly how they would go. | `conformance/vectors/label-004`, on both engines: 8 of 82 fail, the first reading *`expected: الشعاع ⁨15.5⁩ م  but was: الشعاع 15.5 م`*. The vector asserts U+2068 and U+2069 **by number** in the generator, so the expectation is readable as codepoints rather than as invisible characters nobody can see in a diff. `InterpolatedLabelTest` fails too, which is the second place they could be stripped — a client "cleaning up" for display. | A run of Latin digits inside Arabic text is directionally neutral at its edges, so the bidirectional algorithm resolves it against the surrounding paragraph and can move it. Without the isolate `الشعاع 15 م` renders with the number in the wrong place and a two-number string reorders outright — the same bug that made a page indicator reading `5 / 25` display as `25 / 5`. | **Yes — 2026-09-03** |
| 56 | **`labelArgs` dropped from the dependency graph.** Remove the `collect_refs` over `labelArgs`/`constraintMessageArgs` in both engines' compile. | `conformance/vectors/label-005`, which asserts `dependsOn` **directly**: 1 of 82 fails on each engine, *`dependsOn[stems] expected [tag] but was []`*. Nothing else does — and that is the finding. Both engines render a label on demand, so every rendered string stayed correct and all eight other label vectors passed. Had the vector checked only the rendered text, this would have shipped. | The edge is load-bearing three ways, none of them the render: `check_sensitivity_propagation` reads `depends_on`, so a label interpolating a sensitive field would stop being refused; `_check_references` turns an unresolvable label reference into a compile error through it; and a client that re-renders on dependency change stops re-rendering. | **Yes — 2026-09-03** |
| 57 | **A collection screen with no dataset source.** Not injected — found by tapping through the form on a Pixel. `CollectionViewModel` resolved choices through `FormInstance.choices` (correct) and built its `FormInstance` with no `DatasetSource` (not), so every dataset-backed select returned an empty list. | Nothing in the repository. All 502 backend tests, every conformance vector and every composeApp test passed: the UI tests construct `QuestionUi` directly and never build a `FormInstance`, and the engine tests build one with a source. On the handset the region question rendered its label, its hint, and empty space. `DatasetSelectTest` still cannot catch it — what catches it now is `FormCatalog.datasetSourceForSubmission` being the only way to get a source, so a view model that wants one cannot forget to ask. | The wiring between a resolved list and a rendered list is exactly the seam no layer's tests cover — above the engine, below the screen's own state. It is the third time this shape has appeared (breaks 21, 23) and the second time only a device found it. | **Yes — 2026-09-03**, found on hardware |
| 58 | **The exporter reads past `answers()`.** In `backend/app/modules/form_engine/projection.py`, change `instance.answers()` to `instance.answers(include_irrelevant=True)` — which is `FormInstance.values` spelled in the way that would not look wrong in review. | Two tests, one per layer: `test_export.py::test_a_non_relevant_answer_is_retained_in_storage_and_absent_from_the_export` (a household that answered "no" to consent reports the 3 children it had typed first) and `test_export_reads_only_answers.py::test_nothing_asks_for_the_irrelevant_answers_by_keyword`, which names the file and line — 2 failed of 21, `cd backend && pytest tests/test_export.py tests/test_export_reads_only_answers.py` | Form IR §4.4/§5.3. A non-relevant field **retains** its value so an enumerator correcting an earlier answer loses nothing, and export **excludes** it so an analysis contains no answers to questions nobody was asked. The two halves exist for different reasons and the wrong one is the obvious one to reach for: the file has the right row count, the right column names and the right types, and every conclusion drawn from it about who has children is wrong. Item 5 named this as the mistake it would bet on before any of it was written. | **Yes — 2026-09-03** |
| 59 | **A repeat row keyed on its position.** In `backend/app/modules/export/shape.py`, write `"instance_id": f"i{row.index + 1}"` in place of `row.instance_id` — a key derived from where the instance currently sits. | Four tests, and the shape of the failure is the finding: `test_a_repeat_row_is_keyed_on_the_stable_id_and_not_on_its_position` **and** both round trips **and** `test_every_long_row_has_exactly_one_parent_and_the_op_log_agrees` — 4 failed of 13, `cd backend && pytest tests/test_export.py`. The cross-form invariant catching it is the point: docs/project-conventions.md predicted that a flattening error breaks that one and a wrong column does not. | Form IR §2.3/§5.4. Positional addressing resolves against the *current* ordered list and deleting an instance does not renumber storage, so `members[1]` means a different person before and after a delete. An export keyed on position disagrees with yesterday's export about who is who, and a join between the two is silently wrong — every column correct, every row present, household A's members filed under household B. | **Yes — 2026-09-03** |
| 60 | **Unreadability that does not propagate through a `calculate`.** In `projection.py`, stop `_unreadable_fields` walking the topological order, so only paths that arrived as ciphertext are marked. | `test_export.py::test_an_unreadable_value_is_a_token_and_never_a_blank`, reading `assert 0.0 == 'ENCRYPTED'` — 1 failed of 13 | A total over three encrypted incomes evaluates to **0**, and 0 in a CSV is a number rather than a gap anybody can see. The `ENCRYPTED` token exists because every statistical tool treats blank, `NA` and `NULL` as missing and will compute a mean over the readable rows without saying so; a computed field is the one place the token has to be *derived* rather than substituted, and it is the one place it would be forgotten. | **Yes — 2026-09-03** |
| 61 | **A dataset code named through whatever list is newest.** In `backend/app/modules/export/service.py`, hand every record `labels[max(labels)]` instead of its own version's lookups. | `test_export_binding.py::test_a_code_is_named_by_the_list_its_own_version_was_published_against` — `Nyamburi Kati` became `Nyamburi Mpya` for the submission collected under form v1: 1 failed of 5, `cd backend && pytest tests/test_export_binding.py -m db` | Break 42's rule at the far end of the pipeline. A dataset pin belongs to a form version (§3.2), so the same code legitimately means two different names in one export, and resolving through the newest list renames a village nobody could tell had moved. Note this break needs the test data to *disagree*: the seed republishes the village list with `V000023` renamed, exactly as `choice-008`/`choice-009` make two versions disagree, because with identical lists both bindings give the same answer and neither can be wrong. | **Yes — 2026-09-03** |
| 62 | **The `ENCRYPTED` token no longer forces a numeric column to text.** In `backend/app/modules/export/statistical.py`, make `_forced_string` skip its `any(cell == ENCRYPTED …)` branch — the naive writer, which renders a value it cannot type as missing. | `test_export.py::test_a_columns_type_changes_with_the_data_and_the_manifest_says_so`, and note **what** it fails with: `TypeChanged: total_income is planned as numeric and holds 'ENCRYPTED'`. The export refuses rather than producing a file — 1 failed of 21, `cd backend && pytest tests/test_export.py` | docs/project-conventions.md item 5's rule, at the only layer that can break it. A `.dta` or `.sav` has one numeric type and no way to say "unreadable", so a token written into a numeric column is written as **missing** — and every statistical tool then computes a mean over the interviews that happened to be readable, silently. `test_statistical_writers.py` measured what pyreadstat actually does first: it does not coerce to missing, it silently retypes the whole column, which is why the exporter decides the type and verifies it rather than letting the library decide. | **Yes — 2026-09-03** |
| 63 | **Name truncation with no collision serial.** In `statistical.py`, make `_claim`'s `while name.lower() in taken` loop never run, so two names truncating to the same 32 characters both keep it. | `test_names_are_shortened_deterministically_and_never_merged` and `test_shortening_depends_on_the_form_and_not_on_the_data` — 2 failed of 21. The library is the backstop and it holds: pyreadstat raises `DuplicateError` rather than merging, so this is a crash and not a silent merge. Both are worth having; the crash gives no clue which two questions collided. | Stata caps a variable name at 32 characters and readstat enforces only SPSS's 64, so it will write a `.dta` Stata refuses. `household_member_income_monthly_gross` and `..._net` truncate to the same 32 — one column holding two questions' answers, right type, right row count, wrong data. The serial is assigned in plan order, which is a function of the form versions alone, so a do-file written last month finds the same column names this month. | **Yes — 2026-09-03** |
| 64 | **An all-null `date` column left for readstat to type.** In `statistical.py`, always take the object-of-dates branch in `_series` instead of the `NaT` one for a column with no dates in it. Not injected first — **found** by the read-back check the first time the suite ran with a date question in it. | `test_an_unanswered_date_column_is_still_a_date_column`, plus three others: 4 failed of 21. The read-back check reports it precisely — `interview_date was written as date and came back as 'string', not 'double'`. | readstat types a column of nothing but nulls as **string**, so a date question nobody answered produced a text column and the same question answered once produced a date column: one form, two schemas, decided by which interviews happened to have an answer. Nothing else in the repository could have seen it — every value in both files is correct. This is what the "read the file back and check every column is what it was declared as" step is for, and it earned itself on its first run. | **Yes — 2026-09-03**, found rather than injected |
| 65 | **`false` read back as `true`, in `.dta` and `.sav` only.** In `backend/app/modules/export/cells.py`, revert `parse`'s boolean branch to `str(cell) not in ("0", "False", "false")`. Not injected — that *was* the code, and extending the round trip to the statistical writers is what found it. | `test_a_long_export_reads_back_as_the_submissions_it_came_from[dta]` and `[sav]` fail; `[csv]` and `[xlsx]` **pass** — 2 failed of 21. That asymmetry is the whole finding. | A CSV hands back the text `0` and a `.dta` hands back the float `0.0`, and `str(0.0)` is not `"0"`. Every "no" in a boolean question became a "yes", in two of the four formats, with nothing on the face of the file to see. It is exactly the mistake item 5's third question predicted — a type that round-trips through CSV and not through Stata — and the round-trip invariant could only see it once it ran against the Stata writer. A suite that had stayed on CSV would have shipped it. | **Yes — 2026-09-03**, found rather than injected |
| 66 | **The binary-response exemption, widened three ways.** Against `test_openapi_contract.py::test_every_success_response_names_a_schema`, now that `GET /exports/{formId}` returns a zip: (a) let the zip 200 offer `application/json` as well; (b) declare the zip body as `{"type": "object"}` instead of a byte stream; (c) the control — drop `response_model=EvaluateResponse` from `POST /forms/evaluate`, which the exemption must **not** have made legal. | Each fails with the route named. (a) *"offers both a binary and a JSON body (['application/json', 'application/zip']). One response, one shape."* (b) *"declares a zip body that is not a byte stream"*. (c) *"POST /api/v1/forms/evaluate → 200 returns an inline schema"* — break 11 still caught, with the exemption in place. Three breaks, three distinct messages, `cd backend && pytest tests/test_openapi_contract.py` | That an exemption stayed narrow. A file download genuinely has no fields to name, which is why the request side already exempts `application/octet-stream` — but an exemption is the usual way a guard stops guarding, and this one is written the same way as that one for that reason: one media type, declared binary, or it fails. (c) is the row that matters most: it is not a new guarantee, it is proof the old one survived the change. Note the media type is named rather than "anything not JSON", so the next binary response is a decision somebody makes rather than one that slips through. | **Yes — 2026-09-03**, all three |
| 67 | **The export 413 declares the payload instead of the envelope.** In `backend/app/api/v1/exports.py`, declare `413: {"model": ExportTooLarge}` — the inner model — rather than `ExportTooLargeResponse`. | `test_every_declared_error_body_names_a_schema`, naming the route and the fields it found: *"declares `ExportTooLarge`, whose fields are ['found', 'limit', 'message']. FastAPI sends `{"detail": ...}`"* — 3 failed of 7. And the other half is a request, not a document: `test_export_binding.py::test_too_many_submissions_is_a_413_carrying_the_numbers` asserts the body the server actually sends has exactly `{"detail"}` at the top. | Break 13's guarantee on a new route, which is the point of re-running it: FastAPI wraps an `HTTPException`'s `detail` before it reaches the wire, so a declared payload is a contract for a body the server has never returned, and a generated client fails on the first refusal it meets. Worth noting the shape this route made easy to get wrong in a *second* way: `response_class` sets the documented media type for **every** declared response, so naming `ZipResponse` there publishes the 404 and the 413 as `application/zip`. The route declares the base `Response`, whose media type is None, so the refusals fall back to JSON — which is what they are. | **Yes — 2026-09-03** |
| 68 | **The `.sav` string limit removed**, so readstat's silence is trusted again. In `backend/app/modules/export/statistical.py`, set `MAX_STRING_BYTES["sav"]` to None. | `test_export.py`: `test_a_long_answer_survives_a_dta_and_is_refused_by_a_sav` and `test_the_string_limit_is_counted_in_bytes_and_not_characters` — 2 failed of 24 | SPSS's documented maximum for a string variable is 32,767 bytes and readstat writes past it without a word, exactly as it writes a `.dta` variable name Stata refuses: the library implements the format it can and leaves the application's rules to the caller. The alternatives to refusing are both worse — truncating loses an answer to keep a file tidy, and writing it anyway produces a file SPSS may not open, silently. The refusal names the formats that *do* hold the value, so it costs an SPSS user one flag rather than their data. Note the `.dta` half needs no limit at all, and that is measured rather than assumed: over 2,045 bytes readstat writes a `strL`, which `test_statistical_writers.py` asserts by reading the type code out of `<variable_types>`. | **Yes — 2026-09-03** |
| 69 | **The string length counted in characters instead of bytes.** In `statistical.check_string_lengths`, change `len(str(cell).encode())` to `len(str(cell))`. | `test_the_string_limit_is_counted_in_bytes_and_not_characters` — 1 failed of 24, and **only** that one. The ASCII case passes either way, which is the finding. | Both formats size a string in bytes, and 20,000 Arabic characters are 40,000 of them — under the limit counted one way and over it counted the other. A character-counted check waves through exactly the case this product meets first, which is why the test is Arabic and not a long run of `s`. This is the third time the byte/character distinction has decided something in this repository (§3.1's exact key matching, the `.dta` `str#` sizing, and now this). | **Yes — 2026-09-03** |
| 70 | **A source directory dropped out of the gates, three ways.** Against `scripts/check_every_directory_is_gated.py`: (a) remove `../scripts` from the ruff command in `ci.yml` — the hole as it actually existed; (b) narrow mypy back to `app`; (c) create a new top-level directory with a `.py` file in it and no gate. | Each fails with the directory named: *"scripts (python) is gated by types only"*, *"conformance (python) is gated by lint only"*, *"tools (python) is gated by nothing"* — `python scripts/check_every_directory_is_gated.py --strict`, exit 1 on all three. | That every directory holding source is READ by something. `check_ci_runs_every_suite.py` asks whether every suite RUNS, which is a different question and left this one unasked: CI ran `ruff check .` and `mypy app` from `backend/`, so `scripts/` and `conformance/` had never been linted or type-checked once — the API-contract generator, the XLSForm importer's CLI, the dev seed, and every conformance vector generator. It read as covered because the job is called "backend (ruff, mypy, pytest)" and was green. When they were finally checked: 3 ruff errors and 602 mypy errors, one of which was a **wrong return annotation** in `generate_function_matrix.py` that had been there since it was written. (c) is the decay mode rather than the original fault, and it is the one that matters longer: a guard that ignores what it has not seen before becomes the thing it was written to prevent. | **Yes — 2026-09-03**, all three |

## What is not on this list

Guarantees with no break recorded against them. These are the honest gaps —
tests exist for all of them, but none has been watched to fail:

| Guarantee | Test that should catch it | Status |
|---|---|---|
| Recalculation is topologically ordered, ties broken by document order | `conformance/vectors/calculate-*.json` | **Unverified** |
| A media chunk cannot be decrypted at another index or in another file | `backend/tests/test_media_upload.py::test_a_chunk_cannot_be_decrypted_at_a_different_index` | **Unverified** |
| An op referencing a file that has not arrived is accepted and pairs up later, in either order | `backend/tests/test_media_upload.py::test_an_op_referencing_missing_media_is_accepted_and_pairs_up_later` and `::test_a_file_that_lands_before_its_op_also_pairs_up` | **Unverified** |
| The same photograph from two devices produces different ciphertext hashes | `backend/tests/test_media_upload.py::test_the_same_photograph_from_two_devices_has_different_ciphertext_hashes` | **Unverified** |
| A GPS fix worse than the project threshold is refused rather than stored | `shared/core/.../CaptureRulesTest`: `a fix worse than the threshold is refused, and handed back to be shown`, `a fix with no reported accuracy is refused, not trusted` | **Unverified** |
| An encrypted file whose media key is wrapped to nobody is refused at the server | `backend/tests/test_media_upload.py::test_an_encrypted_file_with_no_wrapped_key_is_refused` | **Unverified** |
| The CameraX and AVFoundation capture paths produce a usable photograph | nothing — no automated environment here has a camera | **Unverified**, and untestable as written. The APK builds and all three targets compile; no photograph has been taken through this path on a device or simulator |
| A wrap addressed to another recipient fails to authenticate rather than silently producing garbage | `conformance/crypto/wrap-multi-001.json` tamper cases | **Unverified** |
| A small-order public key is refused as a recipient | `backend/tests/test_project_keys.py::test_small_order_points_are_not_usable_recipients` | **Unverified** |
| A published test keypair is refused outside development | `backend/tests/test_published_test_keys.py` | **Unverified** |
| Migration 0001 round-trips: `downgrade` then `upgrade` yields the identical schema | `backend/tests/test_migrations.py` (`db`-marked) | **Unverified** |
| A revoked key stops receiving wraps but still opens what it already opened | `backend/tests/test_project_keys.py::test_a_revoked_key_stops_receiving_wraps_but_keeps_opening_what_it_has` | **Unverified** |
| The key upload payload carries only `publicKey`, `role` and `label` | `web/src/pages/ProjectKeysPage.test.tsx` | **Unverified** |
| The Android database key is derived inside the Keystore and never lands in a file | `scripts/prove_local_encryption.sh` step 6 — and it is a *structural* check (no `shared_prefs` exists at all), because there is no key material outside the process to search for | **Unverified** — the break would be rewriting `DatabaseKeyStore.android.kt` to seal a random key and store the blob, which is a redesign rather than a one-line break |
| The iOS Keychain item is `WhenUnlockedThisDeviceOnly`, so the key is in no backup and on no second device | nothing — the attribute is set in `DatabaseKeyStore.ios.kt` and never read back | **Unverified**, and there is no test at all. The simulator does not model backup or iCloud Keychain, so this needs a device and a restore to check |
| The app lock actually gates the key on Android and iOS | nothing | **Unverified**, and untestable as written: the keystore key's auth binding is fixed at generation, and no automated environment here has a lock screen to satisfy. `AppLock.ENABLED` is off |
| The desktop Linux (`secret-tool`) and Windows (Credential Manager) stores work | nothing — `MacKeychainCredentialStoreTest` covers macOS against a scratch keychain; CI is a headless Linux runner with no Secret Service daemon | **Unverified**. The Windows implementation has never run on Windows |
| A locked macOS keychain is told apart from an absent key | nothing, and it cannot be from `security(1)`: everything that is not a hit is `errSecItemNotFound`, and a locked keychain answers with an interactive prompt rather than an exit code | **Unverified by design.** Break 9 is what stands in for it — the guard moved up to `createSubmissionStore`, where the *database file* settles the question instead of the keystore |

## A note on the instruments

Break 6 is a negative guarantee — "the key is in no storage and no request" —
and a negative passes just as cheerfully when the instrument is broken. It very
nearly did: under this jsdom, `window.localStorage` reads back `undefined`, so
the break would have **thrown** rather than leaked and the test would have
reported "nothing leaked" about a channel it could not observe.

`web/src/test/harness.test.ts` exists for that reason. It writes to every sink
`watchForEscapes` claims to watch and asserts each one is recorded. When a row
in this register is a negative, check the instrument before trusting the green.

## A note on the method

**Confirm the break is actually in the file before believing either result.**
This is not fussiness; it is the assumption every row above rests on. A break
that never landed produces a green run that reads exactly like a caught
regression reads like a red one — and the green is the dangerous direction,
because it is recorded as "the guard held" when nothing was tested at all.

It has already happened once here. Break 66's control was "drop
`response_model=EvaluateResponse` from `POST /forms/evaluate` and confirm the
contract guard still fails" — the half that proves an exemption did not widen
the guard. It reported **7 passed**, which was read for a moment as the guard
holding. It was not: the edit had matched nothing, because the decorator is on
one line and the replacement expected it on its own line. Applied properly the
run fails and names the route.

So, before running the command in **Caught by**:

- `git diff` the file, or grep for the changed text, and see the break.
- Prefer an edit that **fails loudly when it does not match** — a script that
  asserts the text changed, rather than a `sed` or a replace that silently
  matches nothing.
- Treat an unexpected pass as "the break did not land" until the diff says
  otherwise. That is the likelier explanation, and it is the one that costs
  nothing to rule out.

The same care applies to a break that fails for the *wrong reason* — an import
error, a typo, a fixture that never ran. Read the assertion message, not just
the exit code. A row is only worth what the failure it names actually says.
